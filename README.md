# EmpApp
employee management system
HR Dashboard
Employee Dashboard
Create two dashboard for Employee and HR.
1 Create login page 

Employee/HR dashboard login asper username
2 Dashboard Pagi
u Doughnut chart displaying the number of active and inactive employee
 Bar chart showcasing the total number of leaves taken by employees  
in the past weez
q Gauge chart displaying the total productive hours of all employee
 A table displaying the list of all employee
p A button to create a new employee
3 Employee Detail Pagi
u Displays all the employee's information, including their name, age, date  
of birth, image, blood group, gender, email id, mobile number, and  
leave detail
 Provides an option for HR to edit or delete an employee
4 Create Employee Pagi
u Contains a form to add an employee's role, name, age, date of birth,  
image, blood group, gender, email id, and mobile number
1 Detail Pagi
u Displays all the information added by HÅ
 Displays all leave details of the employeÍ
q Provides an option for the employee to edit their profile
2 Leave Application Pagi
u This page allows employees to apply for leaves by filling out a form  
that includes the type of leave, start date, end date, and reason for  
taking leave. 

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 15.1.3.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via a platform of your choice. To use this command, you need to first add a package that implements end-to-end testing capabilities.

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.io/cli) page.
